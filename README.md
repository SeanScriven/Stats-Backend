# Stats-Backend

### Setup

1. Clone the repo
2. Install dependencies
   pip install httpx
3. Add your API key and set RUGBY_BASE_URL to 'https://v1.rugby.api-sports.io' in an env file 
4. Run the database setup
   python setup_db.py
5. Run the app
   python main.py